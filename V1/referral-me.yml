info:
  name: referral/me
  type: http
  seq: 9

http:
  method: GET
  url: "{{base_url}}/api/v1/referral/me"
  headers:
    - name: accept
      value: "*/*"
    - name: Authorization
      value: Bearer {{token_user}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
