info:
  name: referral/program
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/api/v1/referral/program"
  headers:
    - name: Accept
      value: application/json
    - name: Authorization
      value: Bearer {{token_user}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
