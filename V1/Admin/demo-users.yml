info:
  name: demo-users
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:5001/api/v1/admin/demo-users
  headers:
    - name: Authorization
      value: Bearer {{token_admin}}
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "sub": "demo-user-1",
        "email": "testuser1@test.com",
        "name": "Dummy Demo"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
