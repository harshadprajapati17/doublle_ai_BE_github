info:
  name: demo-admins
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}/api/v1/admin/demo-admins"
  headers:
    - name: Authorization
      value: "Bearer "
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "sub": "demo-admin-2",
        "email": "other@test.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
