info:
  name: demo-users-get
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/v1/admin/demo-users"
  headers:
    - name: Authorization
      value: Bearer {{token_admin}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
