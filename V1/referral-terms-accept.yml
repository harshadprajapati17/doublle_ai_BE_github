info:
  name: referral/terms/accept
  type: http
  seq: 7

http:
  method: POST
  url: "{{base_url}}/api/v1/referral/terms/accept"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{token_user}}
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
