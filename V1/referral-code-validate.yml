info:
  name: referral/code/validate
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/api/v1/referral/code/validate"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "code": "MRTAE6WW"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
